
function create(options) {

    var invaderController = (function(doneCallback){

        var _exports = {};

        var _doneCallback = doneCallback;
        var _physicsModel = null;
        var _view = null;

        // Create the view

        // Create the physicsModel

        //_doneCallback();

        return _exports;

    }(doneCallback));

    return invaderController;
}

